/** Módulo de autenticación (Auth.js + credenciales). */
export { auth, signIn, signOut, handlers } from "@/auth";
export { getAuthErrorMessage } from "@/lib/auth/errors";
export { getSafeCallbackUrl } from "@/lib/auth/callback-url";
