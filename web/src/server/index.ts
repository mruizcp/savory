/**
 * Server-only composition: services, data access, orchestration.
 * Import from Server Components, Route Handlers, or Server Actions — not from client components.
 */

export { prisma } from "@/server/db/prisma";
